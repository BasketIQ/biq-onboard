{
  "name": "biq-onboard-app",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "build:lib": "vite build --config vite.lib.config.ts",
    "typecheck": "tsc",
    "test": "node --test tests/*.test.mjs",
    "stylelint": "stylelint \"src/styles.css\""
  },
  "devDependencies": {
    "stylelint": "^16.10.0",
    "stylelint-config-standard": "^36.0.1",
    "typescript": "^5.8.3",
    "vite": "^7.0.4"
  }
}
